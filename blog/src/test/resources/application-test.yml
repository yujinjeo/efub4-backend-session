# application-test.yml
spring:
  datasource:
    url: jdbc:h2:mem:test #내장 메모리 모드로 동작함
    username : sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
